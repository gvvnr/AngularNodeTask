import { ListOfItemsPipe } from './list-of-items.pipe';

describe('ListOfItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new ListOfItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
